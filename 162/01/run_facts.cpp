#include "state_facts.h"

int main(int argc, int * argv[]) {
    int state_size; string file_name;
    if (argc == 5) {
        struct state * county_array = new state[];
        struct county * county_array = new county[];
    }
    else {
        cout << "Error: Invalid amount of arguments.\n";
    }
	return 0;
}